using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Combo_text : MonoBehaviour
{

    Vector3 target = new Vector3(-4f, 1.5f, 0);
    Vector3 Me;
    // Start is called before the first frame update
 /*   void Start()
    {
        Me = new Vector3(this.transform.position.x, this.transform.position.y, this.transform.position.z);

    }

    // Update is called once per frame
    void Update()
    {
      //  transform.position = Vector3.Slerp(Me, target, 3f);
    }
    */
}
